info:
  name: Health Check
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/health"
  body:
    type: text
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
