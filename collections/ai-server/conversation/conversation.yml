info:
  name: conversation
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/conversation"
  body:
    type: json
    data: |-
      {
          "query": "Hi my name is ayush",
          "model": "gemini-2.5-flash",
          "provider": "Google"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
