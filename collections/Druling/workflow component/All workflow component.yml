info:
  name: All workflow component
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/workflow_component/all/"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
