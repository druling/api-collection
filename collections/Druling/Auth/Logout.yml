info:
  name: Logout
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/logout"
  headers:
    - name: X-Requested-By
      value: Druling
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
