info:
  name: Send Code
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/user/email_verify/send_code/"
  body:
    type: json
    data: |-
      {
          "email": "test2@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
