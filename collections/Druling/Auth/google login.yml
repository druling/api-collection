info:
  name: google login
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/user/google_login/"
  headers:
    - name: X-Requested-By
      value: Druling
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
