info:
  name: tool details
  type: http
  seq: 2

http:
  method: GET
  url: "{{mcp_url}}/tool/gmail"
  headers:
    - name: X-INTERNAL-AUTH
      value: "{{internal_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
