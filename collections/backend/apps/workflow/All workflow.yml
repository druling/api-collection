info:
  name: All workflow
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/workflow/all/"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
