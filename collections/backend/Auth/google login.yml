info:
  name: google login
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/user/google_login/"
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
