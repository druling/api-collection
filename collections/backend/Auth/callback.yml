info:
  name: callback
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/auth/callback/"
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
