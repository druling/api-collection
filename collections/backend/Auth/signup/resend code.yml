info:
  name: resend code
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/auth/resend_code/"
  body:
    type: json
    data: |-
      {
          "id": "email_verify_entity"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
