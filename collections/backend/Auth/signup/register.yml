info:
  name: register
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/auth/register/"
  body:
    type: json
    data: |-
      {
        "first_name": "test",
        "last_name": "test2",
        "email": "test2@gmail.com",
        "password": "asaf"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
