info:
  name: check email
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/auth/check_email/"
  body:
    type: json
    data: |-
      {
          "email": "test2@gmail.com"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
