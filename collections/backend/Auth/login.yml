info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/auth/login/"
  body:
    type: form-urlencoded
    data:
      - name: password
        value: Test@1233
      - name: email
        value: test2@gmail.com

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
