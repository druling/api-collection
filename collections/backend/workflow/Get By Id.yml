info:
  name: Get By Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/workflow/5f64e697-cb95-462a-85a2-700802e67101/"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
