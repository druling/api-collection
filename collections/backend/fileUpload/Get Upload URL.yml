info:
  name: Get Upload URL
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/file/signed_url/"
  body:
    type: json
    data: |-
      {
          "name": "test.pdf",
          "size": 10,
          "type": "pdf"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
