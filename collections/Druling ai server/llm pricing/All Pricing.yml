info:
  name: All Pricing
  type: http
  seq: 1

http:
  method: GET
  url: "{{ai_url}}/pricing/all"
  headers:
    - name: X-INTERNAL-AUTH
      value: "{{internal_token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
